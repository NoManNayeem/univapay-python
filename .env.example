# ===============================================
# Univapay-Python SDK — Example .env
# Copy this file to `.env` and fill in the values.
# Only commit `.env.example`; never commit your real `.env`.
# ===============================================

# ------------------------------------------------
# Core credentials (server-side)
# - UNIVAPAY_JWT    : FE App Token (JWT) issued by Univapay
# - UNIVAPAY_SECRET : Secret key for server-side API auth
# Both are required to make server-side API calls.
# ------------------------------------------------
UNIVAPAY_JWT=replace-with-your-app-jwt
UNIVAPAY_SECRET=replace-with-your-secret-key

# ------------------------------------------------
# Store scope
# If your API routes are store-scoped, set this.
# Example: 11f06e27-a6a8-5c72-836c-17d938e5f8be
# ------------------------------------------------
UNIVAPAY_STORE_ID=

# ------------------------------------------------
# API base URL
# - Production: https://api.univapay.com
# - Mock/local : http://127.0.0.1:4010
# ------------------------------------------------
UNIVAPAY_BASE_URL=https://api.univapay.com

# ------------------------------------------------
# Networking & logging
# ------------------------------------------------
# HTTP request timeout (seconds)
UNIVAPAY_TIMEOUT=30

# SDK debug logging (truthy/falsey: 1/0, true/false, yes/no)
UNIVAPAY_DEBUG=1

# ------------------------------------------------
# Webhook verification (optional, recommended for real webhooks)
# - Set your signing secret and (optionally) the header name and tolerance.
# - Header usually one of: X-Univapay-Signature / X-Univapay-Webhook-Signature
# ------------------------------------------------
UNIVAPAY_WEBHOOK_SECRET=replace-with-your-webhook-secret
UNIVAPAY_WEBHOOK_HEADER=X-Univapay-Signature
UNIVAPAY_WEBHOOK_TOLERANCE=300

# ------------------------------------------------
# Flask demo (optional)
# ------------------------------------------------
FLASK_ENV=development
FLASK_DEBUG=1
PORT=5057

# ------------------------------------------------
# Example defaults (optional helpers used by example scripts)
# ------------------------------------------------
EXAMPLE_DEFAULT_CURRENCY=jpy
# Optional: override widget loader URL (for restricted networks)
UNIVAPAY_WIDGET_URL=
